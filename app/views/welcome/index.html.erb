<% if current_user %>
  <div>Logged in as <%= current_user.name %></div>
  <div>Location: <%= cookies.encrypted[:location] %></div>
<% else %>
  <%= button_to "Create New User", register_path, method: :get %>
  <%= link_to "Log In", login_path %>
<% end %>

<% if current_user %>
  <section class='existing-users'>
      <p>Existing Users: </p>
      <% @users.each do |user| %>
          <div><%= user.email %></div>
      <% end %>
  </section>
<% end %>